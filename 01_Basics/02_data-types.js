"use strict" // treat all code as a newer version

console.log(3
     + 3);     console.log("farhan"); // code readability should be high 

let name = "farhan"
let age = 18 
let male = true
let state;  // undefined value => we can assign it but not define it's value
let value = null // we can define it's value is empty

// number => range 2 to power 52-53
// string => "" or ''
// boolean = true/false
// bigint = big numbers / for stock markete etc
// null = stand alone value / representation of empty value
// undefined => assign but not defined
// symbol => for uniqueness

// checking type of variables 
console.log(typeof "farhan"); // string / we can use value for checking type ---
console.log(typeof age); // number / and also variable names
console.log(typeof undefined); // undefined
console.log(typeof null); // object