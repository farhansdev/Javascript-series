// 3. Write a program to find the index of letter “n” in the word
// “Pakistani” and display the result in your browser

// const findIndex = "Pakistani";

// let indexOn = findIndex.indexOf('n'); // indexOf() method = checking index position in string 

// alert(`The index of n is ${indexOn} in ${findIndex} string`)

const index = "Istanbul";

let indexon = index.indexOf('n');

alert(`the n index on ${indexon} number index`)